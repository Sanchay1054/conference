at line:156 - Paste the scripts first and then the form

First remove the content
Uncomment the fetchURL() at 198
Paste the scripts


<form class="register" method="POST">
            <div class="registertext">
                <div class="fade-in-section registert">
                    <b class="registerheading">Personal Details</b>
                    <div style="display: flex;" class="registerdetail">
                        <div class="salutation"><input type="checkbox" name="salutation" value="Dr."> Dr</div>
                        <div class="salutation"><input type="checkbox" name="salutation" value="Prof."> Prof.</div>
                        <div class="salutation"><input type="checkbox" name="salutation" value="Mr."> Mr.</div>
                        <div class="salutation"><input type="checkbox" name="salutation" value="Mrs."> Mrs.</div>
                        <div class="salutation"><input type="checkbox" name="salutation" value="Ms."> Ms.</div>
                    </div>
                    <div class="registerdetail">
                        First Name<br>
                        <input type="text" name="firstname" id="firstname"required>
                    </div>
                    <div class="registerdetail">
                        Last Name<br>
                        <input type="text"  id="lastname" name="lastname">
                    </div>
                </div>
                <div class="fade-in-section registert">
                    <b class="registerheading">Contact Details</b>
                    <div class="registerdetail">
                        Phone Number (WhatsApp)<br>
                        <input type="text" name="phonenumber" id="phonenumber" required>
                    </div>
                    <div class="registerdetail">
                        Email<br>
                        <input type="email" name="email" id="email" required>
                    </div>
                    <div class="registerdetail">
                        Address<br>
                        <textarea type="text" row="10" cols="30" name="address" id="address" required></textarea>
                    </div>
                </div>
            </div>
            <div class="registertext">
                <div class="fade-in-section registert">
                    <b class="registerheading">Academic Details</b>
                    <div class="registerdetail">
                        Institution<br>
                        <input type="text" name="institution" id="institution" required>
                    </div>
                    <div class="registerdetail">
                        Department<br>
                        <input type="text" name="department" id="department" required>
                    </div>
                    <div class="registerdetail" style="justify-content: center;">
                        Faculty / Research Scholar / Student<br>
                    <select name="category" required>
                        <option value="Faculty" selected>Faculty</option>
                        <option value="Research Scholar">Research Scholar</option>
                        <option value="Student">Student</option>
                    </select>
                    </div>
                    <div class="registerdetail">
                        (If Faculty / Research Scholar) <br>Designation<br>
                    <input type="text" name="designation" placeholder="Enter your designation"  />
                    </div>
                    <div class="registerdetail">
                        (If Student) Student Year<br>
                    <select name="student_year">
                        <option value="I am a Faculty / Reasearch Scholar" selected>I am a Faculty / Research Scholar</option>
                        <option value="1st Year" >1st Year</option>
                        <option value="2nd Year">2nd Year</option>
                        <option value="3rd Year">3rd Year</option>
                        <option value="4th Year">4th Year</option>
                    </select>
                    </div>
                </div>
            
                <div class="fade-in-section registert">
                    <b class="registerheading">Participation Details</b>
                    <div class="registerdetail">
                        Proof for payment<br>(Drive Link of Payment Screenshot with public access)<br>
                        <input type="text" name="payment">
                    </div>
                <div class="registerdetail">
                    Listener/presenter<br>
                    <select name="presenter" required><option value="Listener">Listerner</option><option value="Presenter">Presenter</option></select>
                </div>
                <div class="registerdetail">
                    Mode: Online or Offline<br>
                    <select name="mode" required>
                        <option value="Online" selected>Online</option>
                        <option value="Offline">Offline</option>
                    </select>
                </div>
                <div class="registerdetail">
                    Accomodation for<br>
                    <div>
                        <input type="radio" name="accomodation" value="Not Required" checked />0 day
                        <input type="radio" name="accomodation" value="1 day"  />1 day
                        <input type="radio" name="accomodation" value="2 days"  />2 days
                    </div>
                </div>
                <div class="registerdetail">
                    <input type="checkbox" name="ack" required> I have read and agree with all the <a href="#notice">remarks</a>
                </div>
                </div>
            </div>
                <div class="registertext">
                <div class="form container">
                    <input class="button" value="submit" type="button" id="submit11" onclick="submit1()">
                    <div id="submit">Click the submit button once. This may take few minutes (Please wait, Thank you)</div>
                </div>
            </div>
        </form>

at line: 168

async function fetchurl(){
                try{
                const data = await fetch("https://conference-backend-2.onrender.com/feedbacks");
                document.getElementById("submit").innerHTML = "";
                }
                catch{

                };
            }

at line: 210

async function submit1() {
  // Retrieve input values
  let salutation = Array.from(document.querySelectorAll('input[name="salutation"]:checked'))
    .map(input => input.value);
  let firstname = document.querySelector('input[name="firstname"]');
  let lastname = document.querySelector('input[name="lastname"]');
  let phonenumber = document.querySelector('input[name="phonenumber"]');
  let email = document.querySelector('input[name="email"]');
  let address = document.querySelector('textarea[name="address"]');
  let institution = document.querySelector('input[name="institution"]');
  let department = document.querySelector('input[name="department"]');
  let category = document.querySelector('select[name="category"]').value;
  let designation = document.querySelector('input[name="designation"]').value;
  let student_year = document.querySelector('select[name="student_year"]').value;
  let payment = document.querySelector('input[name="payment"]');
  let presenter = document.querySelector('select[name="presenter"]').value;
  let mode = document.querySelector('select[name="mode"]').value;
  let accomodation = document.querySelector('input[name="accomodation"]:checked').value;
  let ack = document.querySelector('input[name="ack"]');

  // Output data to console (for testing purposes)
//   console.log({
//     salutation,
//     firstname:firstname.value,
//     lastname:lastname.value,
//     phonenumber:phonenumber.value,
//     email:email.value,
//     address:address.value,
//     institution:institution.value,
//     department:department.value,
//     category,
//     designation,
//     student_year,
//     payment:payment.value,
//     presenter,
//     mode,
//     accomodation,
//     ack:ack.checked
//   });
  if (firstname.value.length === 0) {
    alert("First Name is required!");
    firstname.focus();
    return;
  }
  if (lastname.value.length === 0) {
    alert("Last Name is required!");
    lastname.focus();
    return;
  }
  if (phonenumber.value.length === 0) {
    alert("Phone Number is required!");
    phonenumber.focus();
    return;
  }
  if (email.value.length === 0) {
    alert("Email is required!");
    email.focus();
    return;
  }
  if (address.value.length === 0) {
    alert("Address is required!");
    address.focus();
    return;
  }
  if (institution.value.length === 0) {
    alert("Institution is required!");
    institution.focus();
    return;
  }
  if (department.value.length === 0) {
    alert("Department is required!");
    department.focus();
    return;
  }
  if (payment.value.length === 0) {
    alert("Payment Screenshot's Drive link is required!");
    payment.focus();
    return;
  }

  // Perform form submission or additional actions
  if (!ack.checked) {
    alert("You must agree to the terms and conditions!");
    ack.focus();
    return ;
  }

  const data = {
    salutation,
    firstname:firstname.value,
    lastname:lastname.value,
    phonenumber:phonenumber.value,
    email:email.value,
    address:address.value,
    institution:institution.value,
    department:department.value,
    category,
    designation,
    student_year,
    payment,
    presenter,
    mode,
    accomodation,
  }

  try {
    document.getElementById("submit11").disabled = true;
    document.getElementById("submit11").value = "Submitting...";//https://conference-backend-2.onrender.com
    const response = await fetch('https://conference-backend-2.onrender.com/api/register', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json'
        },
        body: JSON.stringify(data)
    });

    if (response.ok) {
        const result = await response.json();
        // document.getElementById('response-message').textContent = 'Form submitted successfully!';
        window.alert("Registration Submitted Successfully.");//\n Kindly check your Email for verification, after sometime
        //console.log(result);
        location.reload(true);
    } else {
        document.getElementById("submit11").disabled = false;
        console.error('Error:', response.status, response.statusText);
    }
} catch (error) {
    document.getElementById("submit11").disabled = false;
    console.error('Error:', error);
}
document.getElementById("submit11").disabled = false;
document.getElementById("submit11").value = "Submit";

  //alert("Form submitted successfully!");
  // Perform the actual submission logic, e.g., send to server
}